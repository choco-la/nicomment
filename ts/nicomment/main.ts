import { bgImgUI } from './backimgui'
import { embedMovie } from './embed_movie'
import { spaceShortcut } from './keyshortcut'
import { mediaUI } from './mediaui'
import { movieJS } from './movie'

movieJS(document)
mediaUI(document)
bgImgUI(document)
embedMovie(document)
spaceShortcut(document)
